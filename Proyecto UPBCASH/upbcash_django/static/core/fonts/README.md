# UPBCash font assets

This folder is reserved for local `.woff2` files used by the UI system.

Recommended files:
- `upbcash-sans.woff2`
- `upbcash-display.woff2`

The current CSS uses safe local fallbacks until branded files are added.
